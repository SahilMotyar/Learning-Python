#learning maths functions:

import math

pi = -3.14
x = 1
y = 3
z = -1

print(round(pi)) #round the number to integer
print(math.ceil(pi)) #round a numebr to upper integer
print(math.floor(pi)) #round a number to lower integer
print(abs(pi)) #gives absolute vale of variable 
print(pow(pi,3)) #gives power of the variable
print(math.sqrt(421)) #finds the square root of the value, gives error for negative values
print(max(x,y,z)) #find the maximum value between the variables 
print(min(x,y,z)) #find the minimum value between the varibales 
